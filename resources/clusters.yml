# Databricks Asset Bundles - Clusters Configuration
# =================================================
# For Databricks Free/Community Edition - SERVERLESS ONLY
#
# This file is optional for serverless-only deployments.
# Jobs and notebooks run on serverless compute by default.
#
# If you need an interactive cluster for development:
# 1. Create one manually in the Databricks UI
# 2. Or uncomment and configure the cluster below

# resources:
#   clusters:
#     dev_cluster:
#       cluster_name: "[${bundle.target}] E-Commerce Analytics - Dev"
#       spark_version: "14.3.x-scala2.12"
#       num_workers: 0
#       spark_conf:
#         spark.databricks.cluster.profile: singleNode
#         spark.master: "local[*]"
#       custom_tags:
#         ResourceClass: SingleNode
#         environment: development
#       autotermination_minutes: 20
#       data_security_mode: SINGLE_USER
#       single_user_name: ${workspace.current_user.userName}
#       node_type_id: Standard_DS3_v2

# =============================================
# SERVERLESS DEPLOYMENT NOTES
# =============================================
#
# For Databricks Free/Community Edition:
# - All notebooks run on serverless compute
# - No cluster definitions required
# - DLT pipelines use serverless: true
# - Jobs execute on serverless automatically
#
# To run notebooks interactively:
# 1. Open notebook in Databricks UI
# 2. Select "Serverless" compute
# 3. Run cells
